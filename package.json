{
  "name": "oneshotfeed",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/index.js" 
  },
  "dependencies": {
    // Удали зависимости Next/React, если они не нужны для твоих API функций
    // Если в api/auth.js ты не используешь сторонние либы, то dependencies можно оставить пустым или удалить вовсе
  }
}
